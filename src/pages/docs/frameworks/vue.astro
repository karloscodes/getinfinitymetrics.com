---
import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout title="Vue.js Integration - Infinity Metrics">
  <h1>Vue.js Integration</h1>
  <p>
    Complete guide to integrating Infinity Metrics with Vue.js applications and
    Vue Router.
  </p>

  <h2>Installation</h2>
  <p>Add the tracking script to your Vue app:</p>
  <pre><code>// public/index.html
&lt;head&gt;
  &lt;script src="https://your-domain.com/infinity.js"&gt;&lt;/script&gt;
  &lt;script&gt;
    Infinity.init(&#123;
      site_id: 'your-site-id'
    &#125;);
  &lt;/script&gt;
&lt;/head&gt;</code></pre>

  <h2>Vue Router Integration</h2>
  <p>Track route changes in Vue Router:</p>
  <pre><code>// router/index.js
import &#123; createRouter &#125; from 'vue-router'

const router = createRouter(&#123;
  // your routes
&#125;);

router.afterEach((to) =&gt; &#123;
  Infinity.track('page_view', &#123;
    path: to.path,
    title: document.title
  &#125;);
&#125;);

export default router;</code></pre>

  <h2>Composition API</h2>
  <p>Create a composable for tracking:</p>
  <pre><code>// composables/useInfinity.js
import &#123; ref &#125; from 'vue'

export function useInfinity() &#123;
  const track = (event, properties) =&gt; &#123;
    if (window.Infinity) &#123;
      window.Infinity.track(event, properties);
    &#125;
  &#125;;

  return &#123; track &#125;;
&#125;</code></pre>

  <h2>Component Example</h2>
  <p>Use the composable in your components:</p>
  <pre><code>// components/Button.vue
&lt;template&gt;
  &lt;button @click="handleClick"&gt;Subscribe&lt;/button&gt;
&lt;/template&gt;

&lt;script setup&gt;
import &#123; useInfinity &#125; from '@/composables/useInfinity'

const &#123; track &#125; = useInfinity()

const handleClick = () =&gt; &#123;
  track('button_click', &#123;
    button_name: 'Subscribe',
    component: 'Header'
  &#125;)
&#125;
&lt;/script&gt;</code></pre>

  <h2>Options API</h2>
  <p>For Options API, use methods:</p>
  <pre><code>// components/Button.vue
&lt;template&gt;
  &lt;button @click="handleClick"&gt;Subscribe&lt;/button&gt;
&lt;/template&gt;

&lt;script&gt;
export default &#123;
  methods: &#123;
    handleClick() &#123;
      this.$infinity.track('button_click', &#123;
        button_name: 'Subscribe'
      &#125;)
    &#125;
  &#125;
&#125;
&lt;/script&gt;</code></pre>

  <h2>Environment Configuration</h2>
  <p>Use environment variables:</p>
  <pre><code>// .env
VITE_INFINITY_SITE_ID=your-site-id
VITE_INFINITY_DOMAIN=your-domain.com</code></pre>

  <p>Next: <a href="/docs/frameworks/wordpress">WordPress Integration</a></p>
</DocsLayout>
