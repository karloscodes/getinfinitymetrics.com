---
const { class: className } = Astro.props;
---

<section id="how-it-works" class={`py-20 md:py-28 bg-white ${className}`}>
  <div class="container px-4 md:px-6">
    <div class="max-w-3xl mx-auto mb-16">
      <div class="flex justify-center items-start">
        <span class="inline-block px-3 py-1 bg-black bg-opacity-5 rounded-full text-sm font-medium mb-4">
          Installation
        </span>
      </div>
      <h2 class="text-3xl md:text-4xl font-black text-center mb-6 text-black">Simple Setup In Minutes</h2>
      <p class="text-gray-600 text-center mt-4 text-base max-w-2xl mx-auto font-medium">
        Get your privacy-focused analytics running in seconds, not hours.
      </p>
    </div>

    <div class="max-w-3xl mx-auto">
      <div class="terminal mb-16 shadow-lg rounded-xl overflow-hidden transition-all duration-300 border border-black" aria-label="Installation command example">
        <div class="flex items-center gap-2 p-3 bg-gray-900 border-b border-gray-700">
          <span class="w-3 h-3 rounded-full bg-red-500"></span>
          <span class="w-3 h-3 rounded-full bg-yellow-500"></span>
          <span class="w-3 h-3 rounded-full bg-green-500"></span>
          <span class="text-gray-400 text-xs ml-2">Terminal</span>
        </div>
        <div class="p-4 md:p-6 pb-10 leading-loose overflow-x-auto text-sm md:text-base">
          <span class="text-gray-400 inline-block mb-1">$</span> curl -fsSL https://getinfinitymetrics.com/install
          -o install.sh && sudo bash install.sh
          <br /><br />
          <span class="text-green-400">✓</span> Infinity Metrics server is running
          at https://your-domain.com/
        </div>
      </div>

      <div class="grid gap-8">
        <article class="bg-white border border-black flex flex-col md:flex-row gap-4 md:gap-6 items-start p-6 md:p-8 rounded-lg shadow-md slide-in">
          <div
            class="w-12 h-12 rounded-md bg-black text-white flex items-center justify-center font-semibold flex-shrink-0 shadow-sm mb-2 md:mb-0"
            aria-hidden="true"
          >
            1
          </div>
          <div class="leading-relaxed">
            <h3 class="text-xl font-black mb-3">
              Get your license
            </h3>
            <p class="text-gray-600 font-medium">
              Purchase your Infinity Metrics license from our <a href="#pricing" class="text-blue-600 hover:underline">pricing section</a> to get started.
            </p>
          </div>
        </article>

        <article class="bg-white border border-black flex flex-col md:flex-row gap-4 md:gap-6 items-start p-6 md:p-8 rounded-lg shadow-md slide-in">
          <div
            class="w-12 h-12 rounded-md bg-black text-white flex items-center justify-center font-semibold flex-shrink-0 shadow-sm mb-2 md:mb-0"
            aria-hidden="true"
          >
            2
          </div>
          <div class="leading-relaxed w-full">
            <h3 class="text-xl font-black mb-3">
              Install with a single command
            </h3>
            <p class="text-gray-600 font-medium">
              Run our streamlined installer and follow the simple setup instructions.
            </p>
             <div class="bg-gray-100 p-3 rounded-md font-mono text-sm overflow-x-auto mt-3 border border-gray-300">
              <code style="white-space: pre-wrap; word-break: break-all;">curl -fsSL https://getinfinitymetrics.com/install -o install.sh && sudo bash install.sh</code>
            </div>
          </div>
        </article>

        <article class="bg-white border border-black flex flex-col md:flex-row gap-4 md:gap-6 items-start p-6 md:p-8 rounded-lg shadow-md slide-in">
          <div
            class="w-12 h-12 rounded-md bg-black text-white flex items-center justify-center font-semibold flex-shrink-0 shadow-sm mb-2 md:mb-0"
            aria-hidden="true"
          >
            3
          </div>
          <div class="leading-relaxed w-full">
            <h3 class="text-xl font-black mb-3">Add the tracking script</h3>
            <p class="text-gray-600 font-medium mb-4">
              Copy our lightweight tracking script to your website(s) for instant data collection.
            </p>
            <div class="bg-gray-100 p-3 rounded-md font-mono text-sm overflow-x-auto border border-gray-300">
              <code style="white-space: pre-wrap; word-break: break-all;">&lt;script defer src="https://your-domain.com/y/api/v1/sdk.js"&gt;&lt;/script&gt;</code>
            </div>
          </div>
        </article>

        <article class="bg-white border border-black flex flex-col md:flex-row gap-4 md:gap-6 items-start p-6 md:p-8 rounded-lg shadow-md slide-in">
          <div
            class="w-12 h-12 rounded-md bg-black text-white flex items-center justify-center font-semibold flex-shrink-0 shadow-sm mb-2 md:mb-0"
            aria-hidden="true"
          >
            4
          </div>
          <div class="leading-relaxed">
            <h3 class="text-xl font-black mb-3">Start capturing insights</h3>
            <p class="text-gray-600 font-medium">
              All set! Access the analytics dashboard at https://your-domain.com/
            </p>
          </div>
        </article>
      </div>
      
      <div class="mt-12 text-center">
        <p class="text-gray-600 font-medium mb-4">Need more detailed instructions?</p>
        <a href="/docs" class="btn-primary py-2 px-6 inline-flex items-center justify-center">
          View Documentation
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M14 5l7 7m0 0l-7 7m7-7H3" />
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Fix line height issues for terminal text */
  .terminal {
    line-height: 1.8;
  }
  
  .terminal div {
    overflow: visible;
  }
  
  article .leading-relaxed {
    line-height: 1.7;
  }
  
  @media (max-width: 768px) {
    .terminal div.p-4 {
      word-break: break-all;
      white-space: normal;
    }
    
    #how-it-works code {
      font-size: 0.8rem;
    }
  }
</style>

<script>
  // Initialize animation on scroll
  document.addEventListener('DOMContentLoaded', () => {
    const steps = document.querySelectorAll('#how-it-works article');
    steps.forEach((step, index) => {
      setTimeout(() => {
        step.classList.add('slide-in');
      }, 300 + index * 150);
    });
  });
</script>
