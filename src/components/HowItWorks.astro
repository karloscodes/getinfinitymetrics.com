<section id="how-it-works" class="py-16 md:py-24">
  <div class="container">
    <div class="max-w-3xl mx-auto mb-16">
      <div class="flex justify-center items-start">
        <h2 class="text-3xl font-bold">One command to install</h2>
      </div>
      <p class="text-gray-600 text-center mt-4">
        Get up and running in seconds, not hours.
      </p>
    </div>

    <div class="max-w-3xl mx-auto">
      <div class="terminal mb-12">
        <span class="text-gray-400">$</span> curl -fsSL https://getinfinitymetrics.com/install
        -o install.sh && sudo bash install.sh
        <br /><br />
        <span class="text-green-400">✓</span> Infinity Metrics server is running
        at https://your-domain.com
      </div>

      <div class="grid gap-8">
        <div class="card flex gap-4 items-start">
          <div
            class="w-10 h-10 rounded-md bg-black text-white flex items-center justify-center font-semibold flex-shrink-0"
          >
            1
          </div>
          <div>
            <h3 class="text-xl font-semibold mb-2">
              Install with a single command
            </h3>
            <p class="text-gray-600">
              Run the installer, follow the instructions.
            </p>
          </div>
        </div>

        <div class="card flex gap-4 items-start">
          <div
            class="w-10 h-10 rounded-md bg-black text-white flex items-center justify-center font-semibold flex-shrink-0"
          >
            2
          </div>
          <div>
            <h3 class="text-xl font-semibold mb-2">Add the tracking script</h3>
            <p class="text-gray-600">
              Copy our lightweight tracking script to your website.<br>
              <code>&lt;script defer src="https://your-domain.com/y/api/v1/sdk.js"&gt;&lt;/script&gt;</code>
            </p>
          </div>
        </div>

        <div class="card flex gap-4 items-start">
          <div
            class="w-10 h-10 rounded-md bg-black text-white flex items-center justify-center font-semibold flex-shrink-0"
          >
            3
          </div>
          <div>
            <h3 class="text-xl font-semibold mb-2">Start capturing insights</h3>
            <p class="text-gray-600">
              All set! Check the dashboard at https://your-domain.com
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
