<!-- Full-width discount banner -->
<div
  class="w-full bg-gradient-to-r from-red-500 to-red-600 text-white py-2 shadow-lg"
>
  <div class="max-w-7xl mx-auto px-4">
    <a
      href="#pricing"
      class="block text-white hover:text-white hover:opacity-90 transition-opacity no-underline"
    >
      <p class="text-sm font-bold text-center text-white">
        Limited Time: Get 75% OFF with the Beta Pricing!
      </p>
    </a>
  </div>
</div>

<section class="py-16 md:py-24 bg-white">
  <div class="container">
    <div class="space-y-12">
      <!-- Text content section - now on top -->
      <div class="text-center slide-in">
        <h1 class="text-4xl md:text-5xl font-black mb-6 text-black">
          Break Free from Trust-Killing Web Analytics.
        </h1>
        <p class="text-black mb-2 max-w-xl mx-auto text-base font-semibold">
          Stop betraying users' trust by shipping their data to third parties.
          Get the insights you need while keeping their data 100% in-house.
        </p>
        <p
          class="text-black mb-6 max-w-xl mx-auto text-lg font-bold text-center"
        >
          <span class="highlight">Go all in First-Party Web Analytics!</span>
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="#pricing"
            class="btn-primary inline-block px-6 py-3 bg-black text-white rounded-md hover:bg-gray-800 transition-colors font-bold"
          >
            Buy License Now
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="h-5 w-5 ml-1 inline-block"
              viewBox="0 0 20 20"
              fill="currentColor"
            >
              <path
                fill-rule="evenodd"
                d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z"
                clip-rule="evenodd"></path>
            </svg>
          </a>
          <a
            href="/why-infinity-metrics"
            class="btn-secondary inline-block px-6 py-3 bg-white border border-black text-black rounded-md hover:bg-gray-50 transition-colors font-bold"
          >
            Learn more
          </a>
        </div>

        <p class="mt-4 text-xs text-gray-500 text-center">
          Try it risk-free: One-time payment & 30-day money-back guarantee.
        </p>
      </div>

      <!-- Video section - replaced screenshots -->
      <div class="max-w-4xl mx-auto">
        <div
          class="bg-white rounded-lg border border-black shadow-md overflow-hidden"
        >
          <div class="relative w-full">
            <div class="video-container relative h-[500px]">
              <div
                class="w-full h-full flex items-center justify-center bg-black"
              >
                <div class="w-full h-full relative">
                  <iframe
                    src="https://www.youtube-nocookie.com/embed/VxBSvtcGlJI?rel=0&modestbranding=1"
                    title="InfinityMetrics Demo Video"
                    class="w-full h-full"
                    frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen></iframe>
                </div>
              </div>

              <!-- Full-screen button -->
              <button
                id="fullscreenBtn"
                class="absolute top-4 right-4 bg-black bg-opacity-70 hover:bg-opacity-90 text-white rounded-full p-2 transition-all duration-200 z-10"
                title="Open in full screen"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  class="h-5 w-5"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M4 8V4m0 0h4M4 4l5 5m11-1V4m0 0h-4m4 0l-5 5M4 16v4m0 0h4m-4 0l5-5m11 5l-5-5m5 5v-4m0 4h-4"
                  ></path>
                </svg>
              </button>

              <div
                class="absolute bottom-0 left-0 right-0 p-4 bg-white border-t border-black text-black text-center"
              >
                <p class="text-sm font-bold">Watch InfinityMetrics in action</p>
              </div>
            </div>

            <!-- Full-screen modal -->
            <div
              id="fullscreenModal"
              class="fixed inset-0 bg-black bg-opacity-95 flex items-center justify-center z-50 hidden"
            >
              <div class="relative w-full h-full max-w-7xl max-h-[90vh] p-4">
                <button
                  id="closeFullscreenBtn"
                  class="absolute top-4 right-4 text-white bg-black bg-opacity-50 hover:bg-opacity-70 rounded-full p-2 z-10 transition-all duration-200"
                >
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="h-6 w-6"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M6 18L18 6M6 6l12 12"></path>
                  </svg>
                </button>
                <div class="w-full h-full">
                  <iframe
                    src="https://www.youtube-nocookie.com/embed/VxBSvtcGlJI?autoplay=1&rel=0&modestbranding=1"
                    title="InfinityMetrics Demo Video - Full Screen"
                    class="w-full h-full"
                    frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen></iframe>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    /* Fix text display issues */
    section {
      overflow: visible;
    }

    h1,
    p,
    span,
    a {
      overflow: visible;
    }

    .slide p {
      padding-bottom: 4px;
    }
  </style>

  <script is:inline>
    document.addEventListener("DOMContentLoaded", () => {
      const fullscreenBtn = document.getElementById("fullscreenBtn");
      const fullscreenModal = document.getElementById("fullscreenModal");
      const closeFullscreenBtn = document.getElementById("closeFullscreenBtn");

      // Open full-screen modal
      fullscreenBtn.addEventListener("click", () => {
        fullscreenModal.classList.remove("hidden");
        document.body.style.overflow = "hidden"; // Prevent background scrolling
      });

      // Close full-screen modal
      closeFullscreenBtn.addEventListener("click", () => {
        fullscreenModal.classList.add("hidden");
        document.body.style.overflow = ""; // Re-enable scrolling
      });

      // Close modal when clicking outside the video
      fullscreenModal.addEventListener("click", (e) => {
        if (e.target === fullscreenModal) {
          fullscreenModal.classList.add("hidden");
          document.body.style.overflow = "";
        }
      });

      // Close modal with Escape key
      document.addEventListener("keydown", (e) => {
        if (
          e.key === "Escape" &&
          !fullscreenModal.classList.contains("hidden")
        ) {
          fullscreenModal.classList.add("hidden");
          document.body.style.overflow = "";
        }
      });
    });
  </script>
</section>
