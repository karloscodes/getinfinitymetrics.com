<section class="py-16 md:py-28 hero-gradient">
  <div class="container">
    <div class="lg:flex lg:items-center lg:gap-16">
      <div class="lg:w-1/2 mb-10 lg:mb-0 text-center lg:text-left slide-in">
        <span class="inline-block px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm font-medium mb-4">
          Pay Once - Own Forever
        </span>
        <h1 class="text-4xl md:text-6xl font-bold leading-tight mb-6 bg-clip-text text-transparent bg-gradient-to-r from-black to-gray-700">
          Self-Hosted, AI-Powered Web Analytics
        </h1>
        <p class="text-gray-600 mb-8 max-w-xl mx-auto lg:mx-0 text-lg leading-relaxed">
          <strong class="text-black">One-time purchase</strong><strong class="text-black">, scale to Infinity.</strong>
          Embedded fast database & near zero maintenance effort.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
          <a href="#pricing" class="btn-primary inline-flex items-center px-7 py-4 bg-black text-white rounded-lg hover:bg-gray-800 transition-colors">
            Join the Beta Program 
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
            </svg>
          </a>
          <a href="#features" class="btn-secondary inline-block px-7 py-4 bg-white border border-gray-300 text-black rounded-lg hover:bg-gray-100 transition-colors">
            Learn More
          </a>
        </div>
      </div>
      <div class="lg:w-1/2">
        <div class="card p-0 overflow-hidden shadow-lg rounded-lg border-0">
          <div class="relative w-full">
            <div class="slideshow-container relative h-[500px]">
              <div class="slide absolute w-full h-full opacity-0 transition-opacity duration-1000 ease-in-out">
                <img src="/dashboard-preview.png" alt="Infinity Metrics AI-Powered Analytics Dashboard" class="w-full h-full object-contain" />
                <div class="absolute bottom-0 left-0 right-0 p-4 bg-gradient-to-t from-black to-transparent text-white text-center opacity-90">
                  <p class="text-sm font-medium leading-relaxed">Simple, effective analytics dashboard</p>
                </div>
              </div>
              <div class="slide absolute w-full h-full opacity-0 transition-opacity duration-1000 ease-in-out">
                <img src="/ask-ai-preview.png" alt="Infinity Metrics AI Assistant Analytics Interface" class="w-full h-full object-contain" />
                <div class="absolute bottom-0 left-0 right-0 p-4 bg-gradient-to-t from-black to-transparent text-white text-center opacity-90">
                  <p class="text-sm font-medium leading-relaxed">Ask your data questions using natural language</p>
                </div>
              </div>
            </div>
            <div class="flex justify-center gap-3 py-3 absolute bottom-14 left-0 right-0 z-10">
              <span class="dot w-3 h-3 bg-white bg-opacity-50 rounded-full cursor-pointer hover:bg-white transition-all" onclick="currentSlide(1)"></span>
              <span class="dot w-3 h-3 bg-white bg-opacity-50 rounded-full cursor-pointer hover:bg-white transition-all" onclick="currentSlide(2)"></span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Fix text display issues */
  section {
    overflow: visible;
  }
  
  h1, p, span, a {
    overflow: visible;
  }
  
  .slide p {
    padding-bottom: 4px;
  }
</style>

<script>
  let slideIndex = 1;
  showSlides(slideIndex);

  function plusSlides(n) {
    showSlides((slideIndex += n));
  }

  function currentSlide(n) {
    showSlides((slideIndex = n));
  }

  function showSlides(n) {
    let i;
    let slides = document.getElementsByClassName("slide");
    let dots = document.getElementsByClassName("dot");
    if (n > slides.length) {
      slideIndex = 1;
    }
    if (n < 1) {
      slideIndex = slides.length;
    }
    for (i = 0; i < slides.length; i++) {
      slides[i].style.opacity = "0";
    }
    for (i = 0; i < dots.length; i++) {
      dots[i].classList.remove("bg-gray-600");
    }
    slides[slideIndex - 1].style.opacity = "1";
    dots[slideIndex - 1].classList.add("bg-gray-600");
  }

  // Auto-advance slideshow
  setInterval(() => plusSlides(1), 5000);
</script>
